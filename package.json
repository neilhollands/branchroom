{
  "name": "branchroom",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first classroom tool for making simple branching videos.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
